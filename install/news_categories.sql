SQL Standard
CREATE TABLE kio_news_categories
(
	category_id INT(10) UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
	category_name VARCHAR(100) NOT NULL,
	category_description TEXT,
	category_entries INT(10),
);

ratings
comments
text_transform
filters